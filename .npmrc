@termx-health:registry=https://npm.pkg.github.com/
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}

# TODO: Remove legacy-peer-deps once ng-zorro-antd@21.x supports Angular 21.
# Currently needed because ng-zorro-antd@20.x peers on Angular ^20, but we use Angular 21.
legacy-peer-deps=true
